# SiteMapper AI（SiteCrawlerAI）— 利用マニュアル

本書は、本ツールを初めて使う方向けの操作マニュアルです。基本的な使い方に加えて、**禁止事項・注意点**もまとめていますので、利用開始前に必ず目を通してください。

対象: 社内の関係者（共有ツールとして提供）

---

## 1. このツールについて

**SiteMapper AI** は、指定したWebサイトを自動巡回（クロール）し、サイト構造やSEO上の問題（リンク切れ・リダイレクト・孤立ページなど）を可視化する内部向けの分析ツールです。クロール結果はダッシュボード上で確認でき、Excel/CSV形式でエクスポートして資料化することもできます。

---

## 2. ログイン方法

本ツールには **HTTP Basic認証**（ブラウザ標準のID/PASSWORDダイアログ）でアクセスします。

1. URLにアクセスすると、ブラウザがID・パスワードの入力ダイアログを表示します
2. 共有されたID・パスワードを入力します（**ID: `bly`**、パスワードは別途共有）
3. 一度入力すれば、ブラウザがその後のアクセスでも資格情報を保持します（同じブラウザ・同じPCであれば毎回入力する必要はありません）

> 独自のログイン画面（メールアドレス登録など）はありません。アクセスURL・ID・パスワードのみで利用できます。

### 注意
- **ID・パスワードを社外の人や無関係な第三者に共有しないでください**。これは社内限定の共有ツールであり、認証情報の流出はツールの不正利用や対象サイトへの過剰アクセスにつながります
- ブラウザに保存された認証情報は端末・ブラウザに紐づきます。共有PCを使っている場合は、利用後にブラウザを閉じる、シークレットウィンドウを使うなど、認証情報が残らないよう配慮してください

---

## 3. 画面構成

ログイン後、左側にサイドバー、上部にヘッダーがあるダッシュボード形式の画面が表示されます。

| メニュー | 内容 |
|---|---|
| ダッシュボード | クロールの開始・概要（KPI）・重大な問題・クロール結果の管理 |
| サイトマップ | サイト構造をツリー表示。孤立ページや階層の深さを確認 |
| ページ一覧 | 全URLを一覧表示。検索・ソート・フィルタが可能 |
| リンク構造 | 内部リンクをグラフで可視化 |
| エラー | 404・500など問題のあるページの一覧 |
| リダイレクト | 3xx転送の一覧・転送チェーン |
| 孤立ページ | どこからも内部リンクされていないページの一覧 |
| エクスポート | 分析結果をExcel/CSVでダウンロード |

ヘッダー右上の歯車アイコンから「Tweaksパネル」を開くと、表示密度や配色などの見た目を調整できます（個人の好みの設定で、分析結果には影響しません）。

---

## 4. 基本的な使い方

### 4.1 クロールを開始する

1. ダッシュボード上部の「新規クロール」欄に、解析したいサイトのURLを入力します（`https://` は自動で補完されます）
2. 「深さ」を選択します（**2 / 3 / 5 / 無制限** から選択）
   - 「深さ」とは、起点URLから何回リンクをたどった先までを巡回するかの上限です
   - 「無制限」は文字どおりサイト全体を巡回しようとするため、**大規模サイトでは非常に時間がかかる**ことがあります（後述の「注意点」も参照）
3. 「クロール開始」ボタンを押すと巡回が始まり、進捗（発見URL数・キュー・速度）がリアルタイムに表示されます
4. クロールが完了すると、ダッシュボードに概要（ページ数・エラー数・リダイレクト数・孤立ページ数・健全性スコアなど）が表示されます

### 4.2 結果を確認する

- ダッシュボードの「重大な問題」「ツール」カードから各画面へ遷移できます
- サイドバーのメニュー項目にある数字バッジは、検出された問題の件数です（赤字はエラー系）
- 各画面では検索・並び替え・フィルタができ、個別のページをクリックすると詳細パネルが開きます

### 4.3 過去のクロール結果を管理する

- ダッシュボード右側の「クロール結果」パネルに、過去に実行したクロールが一覧表示されます
- 行をクリックすると、そのクロール結果に切り替えて閲覧できます（「アクティブ」を切り替える操作です）
- 「再クロール」ボタン（ヘッダー右上）で同じ設定での再実行ができます
- 不要になったクロール結果は、一覧の「×」ボタンから削除できます

### 4.4 結果をエクスポートする

「エクスポート」画面から、分析結果を以下の形式でダウンロードできます。

- **Excel（.xlsx）**: 選択した複数のデータセットを1つのファイルに、シート分けしてまとめて出力
- **CSV（.csv）**: データセットごとに個別のファイルとして出力

監査資料や報告書の元データとして活用してください。

---

## 5. 禁止事項

本ツールは強力な巡回機能を持つため、以下の行為は**禁止**します。

1. **許可を得ていないサイトのクロール**
   自社サイトや、クロールの許可を得ている対象以外のサイト（顧客サイト、競合サイト、その他第三者サイトなど）を無断で解析しないでください。Webサイトへの自動アクセスは、サイト運営者の利用規約や著作権法、不正アクセス関連の法令に抵触する可能性があります。**クロール対象は必ず「自社サイト」または「事前に許可を得ているサイト」に限定してください。**

2. **対象サイトに過剰な負荷をかける目的での利用**
   本ツールは対象サイトへの配慮として、同時接続数やアクセス間隔に制限を設けていますが（後述）、それでも繰り返しクロールを実行するなどして意図的に対象サイトへ負荷をかける行為は禁止します。

3. **取得した情報の目的外利用・外部への持ち出し**
   クロールによって取得した情報（サイト構造、ページ内容、エクスポートしたファイルなど）を、業務上の分析目的以外に利用したり、社外に持ち出したりしないでください。

4. **認証情報の第三者への共有**
   ログイン用のID・パスワードを社外の人や無関係な第三者に教えないでください。

5. **イントラネット・社内限定サイト・ローカル環境への利用の試行**
   本ツールはセキュリティ対策として、`localhost` やプライベートIPアドレス帯（イントラネット等）へのアクセスを技術的にブロックしています。これらに対するクロールは動作しない仕様であり、設定変更などで回避を試みないでください。

---

## 6. 注意点（運用上知っておくべきこと）

- **クロールには時間がかかります**
  サイトの規模や「深さ」の設定によっては、クロールの完了まで数分〜数十分かかることがあります。進捗バーで状況を確認しながらお待ちください。

- **アクセス制御（取得制限）について**
  対象サイトへの配慮として、本ツールは内部的に以下の制限のもとで動作します。
  - 同時接続数: 最大5
  - アクセス間隔: 同一サイトに対して秒間 約10リクエストまで
  - 取得ページ数の上限: 1クロールあたり最大10,000ページ（上限に達した場合は「途中で打ち切られた」旨が表示されます）
  - **robots.txt のルールを尊重します**（対象サイトがクロールを許可していないページは取得されません）

- **「深さ：無制限」は計画的に使用する**
  非常に大きなサイトに対して「無制限」を指定すると、長時間にわたりクロールが継続し、上記の上限（最大10,000ページ）に達して打ち切られる場合があります。まずは「深さ：3」程度の小さい設定で試すことを推奨します。

- **クロール結果の削除は取り消せません**
  クロール結果一覧の「×」から削除すると、そのクロールで取得したデータは完全に削除され、復元できません。削除前には確認ダイアログが表示されますので、必ず内容を確認してください。

- **同時に複数のクロールを実行しない**
  クロール実行中は「クロール開始」ボタンが無効化され、新しいクロールは開始できません。これは対象サイトへの負荷集中を避けるための仕様です。クロールが完了するのを待ってから次の操作を行ってください。

- **共有ツールであることを意識する**
  社内の複数人が同じ環境を利用します。他の人が実行したクロール結果を誤って削除しないよう、操作前にホスト名・ラベルをよく確認してください。

---

## 7. 困ったときは

- クロールが開始できない、画面が正しく表示されないなど、動作に問題がある場合は、システム管理者（本ツールの導入担当者）までご連絡ください
- ログイン情報（ID・パスワード）を忘れた場合も、社外の人に聞かず、必ず社内の管理担当者に確認してください
